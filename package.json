{
  "name": "@dank-inc/numbaz",
  "version": "0.1.0",
  "description": "The Dankest Number Library",
  "files": ["lib"],
  "main": "index.js",
  "scripts": {
    "lint": "tsc --noEmit",
    "compile": "rm -rf lib && tsc",
    "deploy": "npm run compile && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.2.3"
  }
}
